============
Installation
============

At the command line::

    $ easy_install django-instagram-photo-api

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-instagram-photo-api
    $ pip install django-instagram-photo-api
